# 服务端口（默认 3100）
PORT=3100

# MCP 传输方式: http | stdio（默认 http）
# MCP_TRANSPORT=http

# MCP 端点路径（默认 /mcp）
# MCP_PATH=/mcp

# Swagger/API 配置请通过 parse_swagger 工具在运行时传入，无需在此配置

# Assets API（读取 assests 目录）:
# GET  /api/assets           列出文件
# GET  /api/assets/aad.json  获取文件内容
